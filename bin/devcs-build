#!/usr/bin/env bash

source bin/devcs-set || exit 1
source "$DEVCS_LIB_BASHLIB64"

bl64_fs_rm_file "$DEVCS_BUILD_PROD"
bl64_fs_merge_files \
  '0755' \
  "$BL64_LIB_DEFAULT" \
  "$BL64_LIB_DEFAULT" \
  "$DEVCS_BUILD_PROD" \
  "${DEVCS_SRC}/cli/${DEVCS_APP_NAME}.header" \
  "$DEVCS_LIB_BASHLIB64" \
  "${DEVCS_SRC}/cli/${DEVCS_APP_NAME}.env" \
  "${DEVCS_SRC}/cli/${DEVCS_APP_NAME}.bash" \
