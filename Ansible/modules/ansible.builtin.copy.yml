---
- name: ""
  ansible.builtin.copy:
    backup: false
    follow: false
    force: true
    remote_src: ~
    owner: "{{  }}"
    group: "{{  }}"
    mode: "{{  }}"
    setype: "{{  }}"
    src: "{{  }}"
    dest: "{{  }}"
...
